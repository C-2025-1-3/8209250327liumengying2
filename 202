#include<iostream>
using namespace std;
int main() {
	double y;
	double x;
	cin >> x;
	if (0 < x < 1) {
		y = 3 - 2 * x;
	}
	else if (1 <= x < 5) {
		y = 2.0 / (4.0 * x) + 1;
	}
	else if (5 <= x < 10) {
		y = x * x;
	}
	return 0;
}
